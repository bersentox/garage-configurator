# Implementation Boundary

Implement the new block only inside:

`blocks/unified-navigation-v2/`

Do not modify:

* `blocks/unified-navigation/`
* any other blocks
* any shared global styles unless absolutely necessary

Treat `blocks/unified-navigation-v2/` as a completely new standalone block.

Expected output files:

* `unified-navigation.html`
* `unified-navigation.css`
* `unified-navigation.js`

Optional:

* `unified-navigation.t123.html`

The old radial implementation is deprecated and must not be reused.
